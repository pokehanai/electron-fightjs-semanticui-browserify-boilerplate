{{#each items}}
<div class="item">
  <img class="ui avatar image" src="{{image}}">
  <div class="content">
    <a class="header">{{name}}</a>
    <div class="description">Version {{version}}</div>
  </div>
</div>
{{/each}}
